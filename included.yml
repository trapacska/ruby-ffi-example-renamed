include:
- path: new.yml

workflows:
  setup:
    steps:
    - script@1:
        title: test push
  deploy:
    after_run:
    - test #            test defined in bitrise.yml → GHOST + jump (reverse direction)               test
    steps:
    - deploy-to-bitrise-io@2: {}
step_bundles:
  shared:
    steps:
    - script@1: {} #           also test. k
